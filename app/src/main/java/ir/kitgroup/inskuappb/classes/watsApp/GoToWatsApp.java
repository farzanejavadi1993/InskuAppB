package ir.kitgroup.inskuappb.classes.watsApp;

import android.app.Activity;
import android.app.Notification;

public interface GoToWatsApp {
    void go(String url, String pkg, Activity activity);
}
